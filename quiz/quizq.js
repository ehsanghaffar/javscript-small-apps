/**
 * Quiz Question Javascript Part 1
 */

var a = (1, 5 - 1) * 2; // returns 8

var b = (1, 2, 3); // returns 3
// explanation:
// comma operator (,) evaluates the each of the operands from left to right and returns the last one.
